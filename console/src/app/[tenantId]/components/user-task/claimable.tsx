"use client";

import { UserGroup } from "@littlehorse-enterprises/user-tasks-bridge-api-client";

export default function ClaimableUserTasks({
	userGroups,
}: {
	userGroups: UserGroup[];
}) {
		return;
}

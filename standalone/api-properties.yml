com:
  c4-soft:
    springaddons:
      oidc:
        ops:
          - iss: http://localhost:8888/realms/lh
            username-claim: preferred_username
            authorities:
              - path: $.realm_access.roles
              - path: $.resource_access.*.roles
            vendor: keycloak
            tenant-id: lh
            clients:
              - user-tasks-client

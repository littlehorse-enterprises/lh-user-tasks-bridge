name: main
on:
  workflow_dispatch:
  push:
    branches:
      - main

permissions:
  contents: read
  id-token: write
  packages: write

jobs:
  build-and-publish:
    uses: ./.github/workflows/shared-publish-backend.yml
    secrets: inherit

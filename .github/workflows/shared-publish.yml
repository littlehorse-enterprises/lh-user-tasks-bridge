name: build and publish
on:
  repository_dispatch:
    types:
      - build
  workflow_call:
    inputs:
      version:
        type: string
        default: main

jobs:
  test-and-build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Java
        uses: actions/setup-java@v4
        with:
          java-version: "21"
          distribution: "corretto"

      - name: Test and Build Artifact
        run: ./gradlew api:build

      - uses: actions/upload-artifact@v4
        with:
          name: sso-workflow-bridge-api-jar
          path: api/build/libs

  build-ui:
    runs-on: ubuntu-latest
    steps:
      - name: Clone UI
        uses: actions/checkout@v4
        with:
          repository: littlehorse-enterprises/lh-sso-workflow-bridge
          path: lh-sso-workflow-bridge
          ref: main
          token: ${{ secrets.PAT_LH_USER_TASKS_UI }}

      - name: Use Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Build UI
        working-directory: lh-sso-workflow-bridge
        run: |
          npm ci
          npm run build -ws

      - uses: actions/upload-artifact@v4
        with:
          name: sso-workflow-bridge-ui
          include-hidden-files: true
          path:  |
            lh-sso-workflow-bridge/ui/.next/standalone
            lh-sso-workflow-bridge/ui/.next/static
            lh-sso-workflow-bridge/node_modules

  docker:
    runs-on: ubuntu-latest
    needs:
      - test-and-build
      - build-ui
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Download API Artifact
        uses: actions/download-artifact@v4
        with:
          name: sso-workflow-bridge-api-jar
          path: ./api/build/libs/

      - name: Download UI Artifact
        uses: actions/download-artifact@v4
        with:
          name: sso-workflow-bridge-ui
          path: ./

      - name: Build and Publish
        id: publish-image
        uses: littlehorse-enterprises/publish-image@v1
        with:
          image-name: lh-sso-workflow-bridge-api
          registry: ecr
          tags: |
            type=semver,pattern={{version}}
            type=ref,event=branch

      - name: Build and Publish Standalone
        id: publish-standalone-image
        uses: littlehorse-enterprises/publish-image@v1
        with:
          image-name: lh-sso-workflow-bridge-standalone
          dockerfile: standalone/Dockerfile
          github-token: ${{ secrets.GITHUB_TOKEN }}
          tags: |
            type=semver,pattern={{version}}
            type=ref,event=branch

      - name: Login to Quay.io
        uses: docker/login-action@v3
        with:
          registry: quay.io
          username: littlehorse+github
          password: ${{ secrets.QUAY_REGISTRY_SECRET }}

      - name: Get image
        id: get-image
        run: |
          image=$(echo "${{ steps.publish-image.outputs.tags }}" | head -n 1)
          echo "IMAGE=${image}" >> $GITHUB_ENV
      - name: Push image to Quay
        run: |
          docker buildx imagetools create \
            --tag quay.io/littlehorse/lh-sso-workflow-bridge-api:${{ inputs.version }} \
            ${IMAGE}
